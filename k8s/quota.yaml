apiVersion: v1
kind: ResourceQuota
metadata:
  name: three-tier-app
  namespace: three-tier-app
spec:
  hard:
    pods: "10"
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    