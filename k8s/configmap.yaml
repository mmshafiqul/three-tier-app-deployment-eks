apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: three-tier-app
data:
  APP_ENV: "production"
  APP_DEBUG: "false"
  DB_HOST: database-service
  DB_PORT: "5432"
  DATABASE_URL: "postgresql://bmi_user:admin1234@database-service:5432/bmi_db"
  